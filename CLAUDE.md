# AI Customer Support Platform — Project Context

This file gives Claude Code persistent context on this project. Read it fully before making changes. Update the "Current Progress" section at the end of each work session so future sessions pick up where you left off.

## Elevator Pitch

An enterprise-style AI-powered customer support platform combining a Laravel backend for business logic and ticketing, a FastAPI microservice for AI/RAG operations, a React frontend for both the customer chat widget and the admin dashboard, backed by a shared PostgreSQL database (with pgvector) and a pluggable LLM layer (OpenAI, Anthropic, or Groq). Built as a portfolio piece to demonstrate polyglot microservice architecture, applied AI (RAG, sentiment, summarization), clean API design, and enterprise development practices (auth, async queues, testing, deployment).

## Tech Stack

- **Backend (business logic):** Laravel 12, PHP 8.2+, Sanctum for auth, Redis-backed queues
- **AI microservice:** FastAPI, Python 3.11+, SQLAlchemy + Alembic (migrations), pgvector, psycopg2, sentence-transformers
- **Frontend:** React 18 + Vite + TypeScript, axios, react-router
- **Database:** PostgreSQL 15+ with the `pgvector` extension enabled
- **Queue/cache:** Redis
- **AI provider:** chat is pluggable via a strategy pattern in `app/services/llm/` (`base.py` + one module per provider + `factory.py`), selected at runtime by the `LLM_PROVIDER` env var — currently supports `openai`, `anthropic`, and `groq` (Groq's free tier is the default for local dev, since OpenAI/Anthropic both require paid credits). Embeddings use a local `sentence-transformers` model (`all-MiniLM-L6-v2`, 384-dim) via `app/services/embeddings.py` — no per-request API cost, same model for ingestion and query-time retrieval.

## Architecture Rules (non-negotiable)

1. **React talks only to Laravel.** The frontend never calls FastAPI directly. This keeps one clean public API surface.
2. **Laravel talks only to FastAPI for AI work.** Laravel owns tickets, users, auth, and orchestration; it forwards AI-specific requests (chat, ingest, sentiment, summarize) to FastAPI over HTTP.
3. **Service-to-service auth:** Laravel → FastAPI calls are authenticated with a shared secret header (e.g. `X-Internal-Api-Key`), verified in FastAPI's `core/security.py`. This is not user-facing auth — it just proves the request came from Laravel.
4. **One Postgres instance, two owners.** Laravel's Eloquent migrations own the business tables (`users`, `agents`, `tickets`, `messages`). FastAPI's SQLAlchemy models own the vector table (`document_chunks`). Neither service should write migrations for the other's tables.
5. **Anything slow or non-interactive goes through a Laravel queue job** (email polling, summarization, sentiment scoring, document ingestion) — never block an HTTP request on an LLM call longer than a simple chat turn.

## Data Flows

**Chat / RAG flow:** customer sends a message in the widget (identified by a client-generated `visitor_id` from `localStorage`, no auth — the widget is anonymous) → Laravel finds the visitor's open ticket or creates one, logs the message, calls FastAPI `/chat` with the message → FastAPI embeds the query, runs a pgvector similarity search over `document_chunks`, computes the minimum cosine distance across retrieved chunks, builds a RAG prompt, calls the configured LLM provider (`app/services/llm/factory.py`) → returns `{reply, escalate}`, where `escalate` is `true` when no chunks were retrieved or the minimum distance exceeds `CONFIDENCE_DISTANCE_THRESHOLD`. Laravel stores the bot reply as a message, marks the ticket `escalated` if flagged (a failed/unreachable FastAPI call also forces `escalate: true` as a fail-safe), and returns `{reply, ticket_id, escalated}` to React, which shows a human-handoff notice when escalated.

**Document ingestion flow:** an agent uploads a document → Laravel stores the file and queues `IngestDocumentJob` → the job calls FastAPI `/ingest` (throws on failure, so it lands in `failed_jobs` instead of silently succeeding) → FastAPI chunks the text, generates embeddings via `sentence-transformers` (`all-MiniLM-L6-v2`), and writes rows into `document_chunks`.

**Email summarization flow:** a scheduled Laravel command (`email:poll`, `Schedule::command('email:poll')->everyFiveMinutes()` in `routes/console.php`) connects to the support inbox via IMAP (`webklex/laravel-imap`, config in `config/imap.php`), fetches unseen messages in the configured folder, and for each one (wrapped in its own try/catch so one malformed email doesn't abort the batch) creates a **new** `Ticket` (`visitor_id` = sender email — no threading by subject/In-Reply-To, that's a documented future enhancement) and a customer `Message` (body prefixed with `"Subject: {subject}\n\n"`), dispatches `SummarizeEmailJob` and `AnalyzeSentimentJob`, then marks the email seen — only after the ticket/message are successfully created, so a failure leaves it unread for retry on the next poll rather than losing it. `SummarizeEmailJob` calls `AiServiceClient::summarize()` → FastAPI `/summarize` and saves the result on `tickets.summary`. FastAPI's side (`app/api/summarize.py`) is built: short inputs (under ~40 words) are returned unchanged to skip a wasted LLM call; otherwise it calls `get_llm_provider()` (same abstraction as `/chat`/`/sentiment`) with a system prompt asking for a plain-text 2-3 sentence summary (key issue + requested action, no markdown/preamble). Unlike `/sentiment`, it doesn't catch provider exceptions — there's no JSON to fail-parse, and this matches `/chat`'s existing convention of letting `AiServiceClient` handle failures on the Laravel side. Surfaced in the dashboard as an "AI Summary" callout above the message thread in `TicketDetailPage`, shown only when `ticket.summary` is present.

**Sentiment flow:** FastAPI `/sentiment` (`app/api/sentiment.py`) takes `{text}`, sends it to the configured LLM provider (`get_llm_provider()`, same strategy as `/chat` — no separate ML model/library) with a system prompt instructing strict-JSON-only output, parses the response (stripping markdown code fences some providers add despite instructions) into `{label, score}`, and falls back to `{"neutral", 0.5}` on any parse failure or provider error rather than raising — sentiment is a nice-to-have signal, not something that should break the ticket flow. The Laravel side (an async job triggered per inbound customer message, storing the result on the message, rolling it up onto the ticket, and surfacing it on the dashboard queue) is not built yet — see Week 4 below.

**Sentiment scoring (Laravel side):** immediately after `ChatController` creates the customer's `Message` (never the bot's), it dispatches `AnalyzeSentimentJob::dispatch($message)` → the job calls `AiServiceClient::sentiment()` (POST `/sentiment`, same auth/timeout/fallback pattern as `chat()`/`ingest()` — connection failure or non-2xx response returns `{'label' => 'neutral', 'score' => 0.5}` rather than throwing, so a flaky AI service never blocks message creation) → saves `sentiment_label`/`sentiment_score` on the message, then recomputes the ticket's `sentiment_summary` as the worst label across all its customer messages so far (negative > neutral > positive) and saves it if changed. Surfaced in the dashboard as colored badges (`badge-sentiment-<label>`) in both `TicketListPage`'s Sentiment column and `TicketDetailPage`'s header + per-message tags; the sentiment filter dropdown (already built in Week 3) now actually narrows results now that tickets have real `sentiment_summary` values.

**Agent flow:** agent logs in via the React `LoginPage` (`src/dashboard/`), which calls `POST /login` (email/password checked against the `agents` table, returns a Sanctum `plainTextToken` + agent info) → `AuthContext` stores the token/agent in `localStorage` and an axios request interceptor (`src/api/client.ts`) attaches `Authorization: Bearer <token>` to every subsequent request → `ProtectedRoute` (`src/auth/`) guards `/dashboard` and `/dashboard/tickets/:id`, redirecting to `/login` when unauthenticated → both routes render nested inside `DashboardLayout` (single header with logo/agent name+role/logout, via `<Outlet/>`) → `TicketListPage` lists tickets (`GET /tickets`, plain array response) with a logout button (`POST /logout` revokes the token) → clicking a ticket opens `TicketDetailPage` (`GET /tickets/{id}`, messages eager-loaded in chronological order), which lets the agent reply (`POST /tickets/{id}/messages`, `sender: 'agent'`, flips `escalated` → `in_progress`) or mark it resolved (`PUT /tickets/{id}` with `{status: 'resolved'}`, which also hides the reply form and resolve button client-side). Replying to an unassigned ticket auto-assigns it to the replying agent. Non-admin agents only see tickets that are unassigned or assigned to them (`TicketController@index`); admins see all. `EnsureIsAdmin` middleware (aliased `admin` in `bootstrap/app.php`) additionally gates `POST /documents` to admins only. `TicketListPage` has status/priority/sentiment filter dropdowns (`?status=&priority=&sentiment=` on `GET /tickets`).

## Repository Structure

```
ai-customer-support-platform/
├── README.md
├── CLAUDE.md                        # this file
├── docker-compose.yml               # local Postgres + Redis
├── docs/
│   ├── architecture.md
│   ├── api-spec/
│   └── er-diagram.png
├── backend-laravel/
│   ├── app/
│   │   ├── Http/Controllers/Api/
│   │   ├── Http/Middleware/
│   │   ├── Models/
│   │   ├── Services/AiServiceClient.php
│   │   ├── Jobs/          # IngestDocumentJob, AnalyzeSentimentJob, SummarizeEmailJob
│   │   └── Console/Commands/PollSupportInbox.php   # email:poll, scheduled in routes/console.php
│   ├── config/imap.php    # webklex/laravel-imap account config (env-driven)
│   ├── database/migrations/
│   ├── routes/api.php
│   ├── tests/
│   └── .env.example
├── ai-service-fastapi/
│   ├── app/
│   │   ├── main.py
│   │   ├── api/          # chat.py, ingest.py, sentiment.py, summarize.py
│   │   ├── core/         # config.py, security.py
│   │   ├── services/
│   │   │   ├── llm/      # base.py, factory.py, openai_provider.py, anthropic_provider.py, groq_provider.py
│   │   │   └── embeddings.py, retriever.py, chunking.py, prompt_builder.py, sentiment.py, summarizer.py
│   │   ├── db/           # session.py, models.py (DocumentChunk)
│   │   └── schemas/      # sentiment.py, summarize.py (chat/ingest schemas are inline in their routers)
│   ├── tests/
│   ├── requirements.txt
│   └── .env.example
├── frontend-react/
│   ├── src/
│   │   ├── widget/
│   │   ├── dashboard/       # DashboardLayout, LoginPage, TicketListPage, TicketDetailPage
│   │   ├── styles/global.css  # shared navy/blue design system, imported once in main.tsx
│   │   ├── api/
│   │   ├── auth/
│   │   └── App.tsx
│   ├── public/
│   ├── package.json
│   └── .env.example
└── scripts/
    └── setup.sh
```

## Database Schema (high level)

- `users` — customer/end-user accounts
- `agents` — support agents/admins, role field (agent/admin)
- `tickets` — id, user_id (nullable), visitor_id (nullable, indexed — anonymous widget visitors or, for email-originated tickets, the sender's email address), status, priority, sentiment_summary, assigned_agent_id, summary (nullable, AI-generated gist for email-originated tickets), timestamps
- `messages` — id, ticket_id, sender (customer/bot/agent), body, sentiment_label, sentiment_score, timestamps
- `documents` — id, title, source_file, uploaded_by, timestamps
- `document_chunks` — id, document_id, chunk_text, embedding (vector), timestamps

## Environment & Service Communication

- Laravel `.env`: standard Postgres connection vars, Redis connection vars, `AI_SERVICE_URL` (FastAPI base URL), `AI_SERVICE_SECRET` (shared secret sent to FastAPI), `IMAP_HOST`/`IMAP_PORT`/`IMAP_ENCRYPTION`/`IMAP_USERNAME`/`IMAP_PASSWORD`/`IMAP_FOLDER` (support inbox polled by `email:poll`, wired into `config/imap.php`)
- FastAPI `.env`: `LLM_PROVIDER` (`openai` | `anthropic` | `groq`), the matching `<PROVIDER>_API_KEY` / `<PROVIDER>_MODEL` pair (all three pairs may be present; only the selected provider's is required), `INTERNAL_API_KEY` (must match Laravel's `AI_SERVICE_SECRET`), `DATABASE_URL` (same Postgres instance), `CONFIDENCE_DISTANCE_THRESHOLD` (min pgvector cosine distance across retrieved chunks above which `/chat` sets `escalate: true`; calibrated to `0.65` for the `all-MiniLM-L6-v2` model — `0.4` was too strict and escalated most real paraphrased questions, not just poorly-covered ones)
- React `.env`: `VITE_API_BASE_URL` pointing at Laravel only
- Default local ports: Laravel `8000`, FastAPI `8001`, React `5173`, Postgres `5432`, Redis `6379`

## Development Commands

**Laravel:** `php artisan serve` to run, `php artisan test` for PHPUnit, `php artisan migrate` for schema changes, `php artisan queue:work` to process jobs locally.

**FastAPI:** `uvicorn app.main:app --reload --port 8001` to run, `pytest` for tests.

**React:** `npm run dev` to run, `npm run build` for production build.

**Postgres:** enable the extension once per database with `CREATE EXTENSION IF NOT EXISTS vector;`

## Coding Conventions

- Laravel: PSR-12, use Form Requests for validation, keep controllers thin (business logic in Services)
- FastAPI: full type hints, Pydantic schemas for all request/response bodies, one router per resource under `app/api/`; LLM calls always go through the `LLMProvider` strategy in `app/services/llm/` — never call a provider SDK directly from a route. Add a new provider by implementing `LLMProvider` and registering it in `factory.py`.
- React: TypeScript strict mode, functional components with hooks, keep `widget/` and `dashboard/` visually and logically separate even though they share the `api/` client layer and the single shared `styles/global.css` (navy/blue design system: CSS variables, `.btn-primary`/`.btn-secondary`, `.badge`/`badge-<status>`, `.message`/`message-<sender>`, `.empty-state`); dashboard pages share layout via `DashboardLayout` (header/logout, nested under `ProtectedRoute` in `App.tsx`) rather than each page rendering its own header

## Current Progress

Roadmap: 6 weeks, ~20 hrs/week (4 hrs/day, 5 days/week), full feature scope.

- [x] **Week 1 — Foundations:** Laravel auth scaffolding (Sanctum, agents/users); FastAPI `/chat` wired to a pluggable LLM provider (OpenAI/Anthropic/Groq via `app/services/llm/`) with shared-secret auth; end-to-end Laravel → FastAPI → LLM smoke test passing. (Note: `tickets`/`messages` tables and the React widget were still empty stubs until Week 2 below — this box was previously checked prematurely.)
- [x] **Week 2 — RAG core:** `tickets`/`messages` tables + models built from scratch (previously undocumented gap); pgvector document ingestion pipeline (`Document` upload → `IngestDocumentJob` → FastAPI `/ingest` → chunk + embed via `sentence-transformers` → `document_chunks`); retrieval + RAG prompt construction (`retriever.py`, `prompt_builder.py`); full chatbot flow with visitor-tracked tickets (anonymous `visitor_id`, no auth on `/chat`); confidence-based auto-escalation (`CONFIDENCE_DISTANCE_THRESHOLD`, calibrated to `0.65`) that flags a ticket `escalated` on low-confidence retrieval, no chunks, or an unreachable AI service; React chat widget (`ChatWidget.tsx`) built and wired end-to-end with human-handoff messaging
- [x] **Week 3 — Admin dashboard:** real agent auth done (`POST /login`, `POST /logout`, `GET /me` via Sanctum tokens against the `agents` table, seeded via `AgentSeeder`); React login page + `AuthContext` (token/agent in `localStorage`, axios interceptor attaches the bearer token) + `ProtectedRoute` + ticket list page (`GET /tickets`) + ticket detail/reply/resolve flow (`TicketController@show`/`@update` and new `MessageController@store`, previously unimplemented stubs) + status/priority/sentiment filter dropdowns + role-based ticket visibility and `admin`-only `/documents` (`EnsureIsAdmin` middleware) all done
- [x] **Week 4 — Sentiment + email summarization:** FastAPI `POST /sentiment` + `POST /summarize` done (LLM provider abstraction, graceful fallback); Laravel-side sentiment wiring done (`AnalyzeSentimentJob`, worst-case rollup, dashboard badges + working filter); email polling done — `email:poll` command (`webklex/laravel-imap`, scheduled every 5 minutes) creates a new ticket + customer message per unseen email (no threading), dispatches `SummarizeEmailJob` (→ `tickets.summary`) and `AnalyzeSentimentJob`, only marks an email seen after successful ticket/message creation; dashboard shows an "AI Summary" callout on `TicketDetailPage` when present
- [ ] **Week 5 — Testing & hardening:** PHPUnit + pytest coverage **started** (sentiment analysis + email summarization covered, see below); input validation, rate limiting, security pass, latency/caching check still outstanding
- [ ] **Week 6 — Deploy & package:** deploy all three services + Postgres, write README + architecture diagram, record demo video

**Last updated:** 2026-09-04 — automated test coverage for this week's sentiment analysis and email summarization work, done ahead of the Week 5 slot since testing a feature while it's fresh beats deferring it. Laravel: `tests/Feature/SentimentAnalysisTest.php` (`Http::fake()` stubbing `/sentiment` — `AnalyzeSentimentJob` saves label/score onto the message; ticket-level worst-case `sentiment_summary` rollup verified both directions via a closure-based `Http::fake()` that returns a different label per message body; `AiServiceClient::sentiment()` falls back to `{'label' => 'neutral', 'score' => 0.5}` on both a non-2xx response and a simulated `ConnectionException`) and `tests/Feature/EmailSummaryTest.php` (`SummarizeEmailJob` saves the summary onto the ticket; `AiServiceClient::summarize()` falls back to a `Str::limit($text, 200)` truncation on both failure modes) — all against real `Ticket`/`Message` models (no factories existed for either, so tests use `::create()` directly). FastAPI: added `pytest`/`httpx` (httpx was already present as FastAPI's TestClient dependency; only `pytest` was actually missing) and regenerated `requirements.txt` via `pip freeze`; `tests/test_sentiment.py` and `tests/test_summarize.py` use `TestClient` + `unittest.mock.patch` on `get_llm_provider` (patched at the point of use — `app.services.sentiment.get_llm_provider` / `app.services.summarizer.get_llm_provider` — not at the factory's own module, since both import it by name) — covering a mocked-success 200, `analyze_sentiment`'s fallback to neutral/0.5 on both plain-invalid and markdown-fence-wrapped-invalid provider JSON, and `/summarize`'s short-input short-circuit asserting the provider mock is never called. One deliberate deviation from the plan's literal wording, consistent with prior sessions' documented finding: the "without header" case asserts **422**, not 401 — `verify_internal_api_key`'s `Header(...)` with no default makes FastAPI's own request validation reject a fully-missing header before the dependency ever runs; 401 is reserved for a present-but-wrong key, already established behavior for `/chat`/`/sentiment` before this session and re-confirmed here rather than re-litigated. Two real environment gaps were found and fixed while getting the Laravel suite to run at all (not app-code bugs, but they blocked every DB-touching Feature test, including ones from prior sessions that had just never been exercised): the local PHP CLI had `pdo_sqlite` disabled in `php.ini` (needed for `phpunit.xml`'s `DB_CONNECTION=sqlite`/`:memory:` test config) — enabled it; and `webklex/laravel-imap`'s pytest equivalent gap didn't apply here since email-poll itself has no automated tests yet (out of scope for this session, per the plan's literal file list). No application code needed changing — all 5 new Laravel tests and 7 new FastAPI tests passed on the first write, and both full suites (`php artisan test`, `pytest`) were run twice in a row with identical results (11 Laravel / 7 FastAPI tests passing each time), confirming no `Http::fake()` state leakage between tests and no order-dependence.
Earlier: email ingestion: `email:poll` command (`app/Console/Commands/PollSupportInbox.php`, `Schedule::command(...)->everyFiveMinutes()` in `routes/console.php`) using `webklex/laravel-imap` (not `webklex/php-imap` as literally named in the plan — that package has no Laravel integration/config at all; the actual Laravel-wired package with a publishable `config/imap.php` and `Client` facade is `webklex/laravel-imap`, confirmed by inspecting both packages' source before installing, same "plan assumed something that turned out not to match reality" pattern as before). Migration adds a nullable `tickets.summary` column. `AiServiceClient::summarize()` added (same pattern as `chat()`/`sentiment()`/`ingest()`, falls back to a 200-char truncated snippet on failure). New `SummarizeEmailJob` saves the FastAPI summary onto the ticket. The command fetches unseen messages, and per email (isolated in its own try/catch) creates a **new** ticket + customer message (visitor_id = sender's email address, reusing that column rather than adding a new one; deliberately no threading by subject/In-Reply-To — noted as a future enhancement, not built), dispatches both `SummarizeEmailJob` and `AnalyzeSentimentJob`, and only calls `setFlag('Seen')` after both are dispatched, so a failure partway through leaves the email unread for retry. `TicketDetailPage` shows an `.ai-summary-callout` box above the message thread when `ticket.summary` is present. No real IMAP test inbox was available this session, so full live polling wasn't verified end-to-end against a real mail server — instead verified: the command correctly resolves the `Client` facade/config and fails with a clean, expected `ConnectionFailedException` against a placeholder host (confirming the wiring up to the network call); and the per-email business logic (ticket/message creation, no-threading behavior for two emails from the same sender, both queued jobs) verified directly via tinker simulation — summary and sentiment both populated correctly end-to-end through the real queue and real FastAPI, including the short-input short-circuit surviving the full job chain unchanged. Live IMAP connection/fetch/read-marking itself should be re-verified against a real inbox before relying on it in production.
Earlier: FastAPI `POST /summarize` (`app/api/summarize.py`, `app/services/summarizer.py`, `app/schemas/summarize.py`): same pattern as `/sentiment` — `get_llm_provider()`, `verify_internal_api_key`, one router mounted in `main.py` — but simpler, since summarizing has no strict output format to parse/fall back on. Inputs under ~40 words are returned unchanged rather than spending an LLM call. Deliberately does *not* catch provider exceptions (unlike `/sentiment`'s JSON-parse fallback) since there's nothing to validate — this matches `/chat`'s existing convention of letting `AiServiceClient` on the Laravel side handle provider failures, confirmed by checking `chat.py` doesn't catch them either. Verified directly against FastAPI: a realistic 150+ word rambling order-complaint email produced an accurate, genuinely condensed 2-sentence summary (not a copy) on Groq; a one-sentence input came back unchanged, confirming the short-circuit; missing/wrong auth header behaved identically to every other endpoint (422/401). Cross-provider testing hit the same pre-existing Anthropic (invalid key) and OpenAI (zero credits) account issues as `/sentiment` — expected 500s given this endpoint doesn't swallow provider errors, not a new problem.
Earlier: sentiment wired into the message flow end-to-end: `AiServiceClient::sentiment()` (same pattern as `chat()`/`ingest()`, falls back to neutral/0.5 rather than throwing), new `AnalyzeSentimentJob` (saves label/score on the customer message, recomputes the ticket's `sentiment_summary` as the worst label across its customer messages), dispatched from `ChatController` right after the customer `Message` is created (never for bot/agent messages). Dashboard shows `badge-sentiment-<label>` pills in `TicketListPage`'s new Sentiment column, `TicketDetailPage`'s header, and per-message tags. Verified live: a frustrated message → message `negative` + ticket `sentiment_summary` `negative`; a calm/positive message on a fresh ticket → `positive`; a ticket with one negative among calm messages still rolls up to `negative` (worst-case, confirmed via DB + UI); the sentiment filter dropdown (built in Week 3 but inert until now) actually narrows the list; stopping FastAPI mid-flow still lets `/chat` succeed end-to-end with sentiment silently falling back to neutral and zero `failed_jobs`.
Earlier: FastAPI `POST /sentiment` (`app/api/sentiment.py`, `app/services/sentiment.py`, `app/schemas/sentiment.py`): reuses `get_llm_provider()` exactly like `/chat` (no separate ML model), asks for strict single-line JSON, strips markdown code fences some providers add anyway, and falls back to `{"neutral", 0.5}` on any parse or provider failure. Verified directly against FastAPI (bypassing Laravel, matching how `/chat`/`/ingest` were tested): negative/positive/neutral messages differentiate sensibly on Groq (the working default); the plan's expectation of a 401 for a *missing* auth header turned out not to match reality — `verify_internal_api_key` (reused unmodified, shared by every endpoint) returns 422 for a missing header via FastAPI's own request validation and 401 only for a *present-but-wrong* value, confirmed identical on the pre-existing `/chat` endpoint, so `/sentiment` is genuinely consistent, just not matching that specific assumption. Cross-provider testing surfaced that both Anthropic (invalid API key) and OpenAI (zero account credits, same issue documented earlier in this file) currently fail for reasons unrelated to this endpoint — real proof, not just Groq's clean case, that the fallback correctly swallows arbitrary provider/auth errors rather than 500ing.
Earlier: visual polish pass + shared dashboard layout: replaced the earlier per-page `dashboard.css` (dark red/black) and `ChatWidget.css` containers with one shared `src/styles/global.css` (navy/blue design system — palette variables, `.btn-primary`/`.btn-secondary`, form resets, `.badge`/`badge-<status>`, `.message-thread`/`.message-<sender>`, `.empty-state`), imported once in `main.tsx`; new `DashboardLayout` (header + `<Outlet/>`) removes the duplicated header/logout markup `TicketListPage` and `TicketDetailPage` each used to render; `ChatWidget` gained a `sending` state (typing indicator, disabled input/button) while its own per-message bubble classes were deliberately left unchanged (customer messages stay right-aligned/blue in the widget — a different convention than the ticket-detail thread's agent-perspective layout, which is why those two contexts intentionally don't share the same message classes for bubbles, only the container system). Verified via a headless-browser (Playwright) run + screenshots (not just reading the CSS) and caught two real regressions this way: `#root`'s scaffold constraints (from `index.css`, previously scoped off via `#root:has(.dashboard-page)` in the now-deleted `dashboard.css`) had to be re-scoped to `#root:has(.dashboard-shell), #root:has(.dashboard-page)`, and a plain `<h1>Tickets</h1>` with no wrapping class was inheriting the scaffold's giant near-black heading style until a `.dashboard-content h1` rule was added.
Earlier: admin dashboard (`LoginPage`/`TicketListPage`/`TicketDetailPage`) restyled with a dark red/black theme (Inter font, colored status/priority badges) in `dashboard.css`; the customer-facing `ChatWidget` and home page were intentionally left untouched. Caught two real bugs while verifying with screenshots rather than trusting the code alone: the global scaffold `index.css` (`#root { max-width: 1126px; border-inline; text-align: center }`) was bleeding into the dashboard, causing white margins on a dark page — scoped it off with `#root:has(.dashboard-page)`; and two of three page `<h1>` rules never set `color` explicitly, so they inherited the scaffold's near-black `h1 { color: var(--text-h) }` instead of the theme's light text, rendering almost invisible.
Earlier: reviewed a set of changes made outside a Claude Code session (`EnsureIsAdmin` middleware gating `/documents`, role-based ticket visibility in `TicketController@index`, auto-assignment in `MessageController@store`, dashboard filter dropdowns): found the frontend didn't actually build (`TicketListPage.tsx` referenced `TicketFilters` without importing it, and `setFilters` was never wired to any UI control) — added the missing import and the three filter `<select>`s. Testing then surfaced a real bug: `TicketListPage`'s data-fetch `useEffect` had no stale-response guard, so React StrictMode's double-invoked effect (or just two overlapping requests from rapid filter changes) could let an older, unfiltered response overwrite a newer filtered one — fixed with a `stale` flag in the effect cleanup. Verified live: role-based visibility (non-admin agent only sees unassigned/own tickets, admin sees all), `/documents` 403s for non-admins, and the filter dropdowns actually filter (confirmed via network-level request/response inspection, not just the rendered list) after the fix.
Earlier: ticket detail view + agent reply flow: `TicketController@show` (eager-loads `messages` ordered chronologically, switched to route-model binding) and `@update` (status transitions, e.g. `resolved`) were both still unimplemented stubs despite being assumed done by the plan; new `MessageController@store` creates agent replies and flips `escalated` → `in_progress`. React `TicketDetailPage` (thread view, reply form, "Mark resolved" button, both hidden once resolved) fetched via `fetchTicket`/`replyToTicket`/`updateTicketStatus` in `ticketsApi.ts`, linked from `TicketListPage`. Verified with a headless-browser (Playwright) run: direct nav to a ticket detail URL while logged out redirects to `/login`, reply appears without a page reload and flips status, resolving hides the reply form/button.
Earlier: React login/dashboard wired to the real auth + tickets APIs: `AuthContext` (login/logout, localStorage-persisted session), an axios interceptor attaching the bearer token to every request, `ProtectedRoute`, `LoginPage`, and `TicketListPage`; `TicketController::index()` was still an unimplemented stub (assumed done by the plan) and had to be filled in to actually return tickets. Verified with a headless-browser (Playwright) run: login → dashboard redirect showing real DB tickets, direct `/dashboard` access while logged out redirecting to `/login`, wrong-password inline error, and logout revoking the session — not just API-level checks.
Earlier: real agent authentication added (`AuthController` with `login`/`logout`/`me`, Sanctum tokens issued against the `agents` table via `HasApiTokens`, `AgentSeeder` for a default admin), replacing the earlier tinker-minted-token workaround used to test `/documents`. Confidence-based ticket escalation implemented end-to-end (FastAPI computes escalate from RAG retrieval distance, Laravel creates/reuses tickets by visitor_id and marks them escalated, React widget shows a handoff notice); this also surfaced and filled in that `tickets`/`messages` schema and the chat widget were never actually built despite earlier checklist claims.

When resuming a session, check the boxes above, look at git log / recent commits for what's actually been built, and continue from there rather than assuming this checklist is perfectly in sync with the code.
